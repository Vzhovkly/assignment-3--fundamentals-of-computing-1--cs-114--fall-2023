// In this line of code we are asking the user to "Enter the size of the matrix:" 
// and then we are converting their input into an iteger using int, which creates and manipulates the matrix

size = int(input("Enter the size of the matrix: "))
